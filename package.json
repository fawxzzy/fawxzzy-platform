{
  "name": "@fawxzzy/platform-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic contracts for the consolidated Fawxzzy platform",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "bootstrap:generate": "node scripts/generate-target-bootstrap.mjs",
    "bootstrap:verify": "node scripts/verify-target-bootstrap.mjs",
    "test": "node --test --test-concurrency=1",
    "validate": "node scripts/validate-contracts.mjs",
    "verify": "node scripts/verify-repository.mjs && node scripts/verify-target-bootstrap.mjs"
  },
  "devDependencies": {
    "ajv": "8.17.1"
  }
}
